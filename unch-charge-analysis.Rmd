---
title: "UNC Health Charges"
author: "Andrew Kant"
date: "8/25/2021"
output: html_document
<<<<<<< HEAD

=======
>>>>>>> 66efe543af3f9a1fd2aa7cc5bda8d4923250c749
---

```{r setup, echo=FALSE}
library(tidyverse)
library(janitor)
library(hrbrthemes)
library(ggrepel)

charges <- read_csv("data/561118388_uncmedicalcenter_cdmstandardcharges.csv")
```

# Introduction


# Data

Let's take a quick look at the data with `glimpse`

```{r}
charges
#clean column names
charges <- clean_names(charges)

glimpse(charges)
```

So it looks like we have charge bundles, standard prices, ranges (min, max) and payor-specific charges in this dataset. 

## Wrangle

Let's organize Payor columns into a more manageable format.

```{r wrangle_col}
charges_cln <- charges %>%
  # Wrangle payor columns to key:value pair
  pivot_longer(
    cols = aetna:united_healthcare_medicare_advantage,
    names_to = "payor",
    values_to = "charge"
    )
```


Looks like most our charge data is not numeric. 

```{r convert-charge-cln}
# Remove $ and , from string, convert to numeric
charges_cln$price = as.numeric(gsub("[\\$,]", "", charges_cln$price))
charges_cln$min = as.numeric(gsub("[\\$,]", "", charges_cln$min))
charges_cln$max = as.numeric(gsub("[\\$,]", "", charges_cln$max))
charges_cln$charge = as.numeric(gsub("[\\$,]", "", charges_cln$charge))
```

```{r convert-charge}
# Remove $ and , from string, convert to numeric
charges$price = as.numeric(gsub("[\\$,]", "", charges$price))
charges$min = as.numeric(gsub("[\\$,]", "", charges$min))
charges$max = as.numeric(gsub("[\\$,]", "", charges$max))
charges$bcbs = as.numeric(gsub("[\\$,]", "", charges$bcbs))
```

```{r index}
charges <- tibble::rowid_to_column(charges, "index")
charges_cln <- tibble::rowid_to_column(charges_cln, "index_cln")
```


```{r top10, warning = FALSE}
charges %>%
  select(index, price, charge_description, cpt_hcpcs) %>% 
  slice_max(price, n = 10) %>% 
  ggplot(., aes(), group = 10) + 
    geom_col(aes(x = charge_description, y = price)) + 
      scale_x_discrete(label = function(x) stringr::str_trunc(x, 25)) +
#coord_flip() + 
      labs(title = "Top 10 Most Expensive Procedures",
           subtitle="UNC Medical Center, Aug 2021",
           x = "",
           y = "Price (US Dollars)") + 
      guides(x = guide_axis(n.dodge = 5)) +
      theme_ipsum_rc()
```

```{r all-std-charges, warning = FALSE}
charges %>%
  select(index, price, charge_description, cpt_hcpcs) %>% 
  ggplot(aes(x = index, y = price, label = charge_description)) + 
    geom_point(aes(), size = 1, alpha = 0.1) +
    # Highlight procedures > $75,000 in red
    geom_point(data = charges[charges$price > 75000,], alpha = 0.5, color = "red", size = 2,) +
    # Only label procedures > $75,000 and avoid overlap
    geom_text_repel(data = charges[charges$price > 75000,], 
      size = 2.5,
      nudge_x = .15,
      box.padding = 0.5,
      nudge_y = 1,
      segment.curvature = -0.1,
      segment.ncp = 3,
      segment.angle = 20
      ) +
    # Add Title, subtitle and axis labels
    labs(title = "Charge Bundles at UNC Health",
      subtitle="UNC Medical Center, Aug 2021",
      x = "Procedure Code",
      y = "Price (US Dollars)") + 
    # Add theme from hrbrthemes lib
    theme_ipsum_rc() + 
    coord_cartesian(clip = "off")
```  

```{r payor-compare}
ggplot(charges_cln, aes(x = charge, y = payor)) +
  geom_density_ridges(rel_min_height = 0.01) +
  scale_y_discrete(expand = c(0, 0)) +
  scale_x_continuous(limits = c(1, 1000), expand = c(0, 0.01)) +
  scale_fill_brewer(palette = 4) +
    labs(
        x = "Price (USD)",
        y = "Payor",
        title = "Healthcare Insurance Comparison",
        subtitle = "Care Charge Bundles (n = 5011)",
        caption = "Andrew Kant | Source: UNC Health") + 
  theme_ridges() + theme(legend.position = "none")

```




